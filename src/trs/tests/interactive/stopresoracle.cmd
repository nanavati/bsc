# $stop resume under the oracle: the QUIET jit secondary yields at
# the same $stop instant, the lockstep+state compare passes at the
# pause, and both engines resume to the $finish.
source stopres.cmd
