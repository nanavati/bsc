# Flagship debug config (docs/TCL-CAPI.md): interp primary with full
# introspection + QUIET aot secondary (artifact-pair construction,
# warm bodies from t=0), lockstep-compared at every stop.  The full
# gcd stepping session must stay byte-identical to the single-engine
# reference.
source gcd.cmd
