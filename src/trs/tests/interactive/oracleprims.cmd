# Fifo-bearing oracle witness: the prims session on interp + QUIET
# aot secondary — the architectural-state compare walks the FIFO
# storage through the arena mirror at every stop (the staleness fix:
# sym_read_range refresh()es from the arena; pre-fix the aot engine
# answered the 0xAA init pattern forever).
source prims.cmd
