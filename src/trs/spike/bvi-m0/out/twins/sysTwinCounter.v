//
// Generated by Bluespec Compiler, version untagged-g424b30802 (build 424b30802)
//
// On Sat Aug 22 04:43:59 UTC 2026
//
//
// Ports:
// Name                         I/O  size props
// CLK                            I     1 clock
// RST_N                          I     1 reset
//
// No combinational paths from inputs to outputs
//
//

`ifdef BSV_ASSIGNMENT_DELAY
`else
  `define BSV_ASSIGNMENT_DELAY
`endif

`ifdef BSV_TASKS_DELAY
`else
  `define BSV_TASKS_DELAY #0
`endif

`ifdef BSV_POSITIVE_RESET
  `define BSV_RESET_VALUE 1'b1
  `define BSV_RESET_EDGE posedge
`else
  `define BSV_RESET_VALUE 1'b0
  `define BSV_RESET_EDGE negedge
`endif

module sysTwinCounter(CLK,
		      RST_N);
  input  CLK;
  input  RST_N;

  // register n
  reg [2 : 0] n;
  wire [2 : 0] n$D_IN;
  wire n$EN;

  // ports of submodule c
  wire [7 : 0] c$bump_amt, c$count;
  wire c$EN_bump, c$RDY_bump;

  // submodule c
  BviCounter c(.CLK(CLK),
	       .RST_N(RST_N),
	       .bump_amt(c$bump_amt),
	       .EN_bump(c$EN_bump),
	       .RDY_bump(c$RDY_bump),
	       .count(c$count));

  // register n
  assign n$D_IN = n + 3'd1 ;
  assign n$EN = c$RDY_bump ;

  // submodule c
  assign c$bump_amt = 8'd3 ;
  assign c$EN_bump = c$RDY_bump ;

  // handling of inlined registers

  always@(posedge CLK)
  begin
    if (RST_N == `BSV_RESET_VALUE)
      begin
        n <= `BSV_ASSIGNMENT_DELAY 3'd0;
      end
    else
      begin
        if (n$EN) n <= `BSV_ASSIGNMENT_DELAY n$D_IN;
      end
  end

  // synopsys translate_off
  `ifdef BSV_NO_INITIAL_BLOCKS
  `else // not BSV_NO_INITIAL_BLOCKS
  initial
  begin
    n = 3'h2;
  end
  `endif // BSV_NO_INITIAL_BLOCKS
  // synopsys translate_on

  // handling of system tasks

  // synopsys translate_off
  always@(negedge CLK)
  begin
    `BSV_TASKS_DELAY;
    if (RST_N != `BSV_RESET_VALUE) $display("count=%0d", c$count);
    if (RST_N != `BSV_RESET_VALUE)
      if (c$RDY_bump && n == 3'd3) $finish(32'd0);
  end
  // synopsys translate_on
endmodule  // sysTwinCounter

